{
	"name": "monorepo",
	"scripts": {
		"docs.dev": "npm --prefix ./docs run dev",
		"docs.install": "npm --prefix ./docs install",
		"docs.build": "npm --prefix ./docs run build",
		"docs.preview": "npm --prefix ./docs run preview",
		"components.dev": "npm --prefix ./packages/components run build",
		"components.install": "npm --prefix ./packages/components install",
		"install": "concurrently 'npm:docs.install' 'npm:components.install'",
		"dev": "concurrently 'npm:docs.dev' 'npm:components.dev'"
	},
	"devDependencies": {
		"concurrently": "^9.2.0",
		"prettier": "^3.6.2",
		"prettier-plugin-svelte": "^3.4.0",
		"prettier-plugin-tailwindcss": "^0.6.14"
	}
}
