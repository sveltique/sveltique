import Option from "./Option.svelte";
import Root from "./Select.svelte";

export { type OptionVariants, option, type SelectVariants, select } from "./variants.js";
export const Select = {
	Root,
	Option
};
