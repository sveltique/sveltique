import { tv } from 'tailwind-variants';

export const badge = tv({
	base: 'relative shrink-0 font-medium inline-flex gap-1 overflow-hidden whitespace-nowrap items-center justify-center bg-blue-100 text-blue-700 rounded-lg px-2 py-1 text-xs'
});
